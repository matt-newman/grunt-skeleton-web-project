var _mn = _mn || {};

_mn.domReady = function() {
    //TODO: //call functions that need to run after content is there
    
};

_mn.testing = function() {
    return 'test';
};

window.addEventListener('DOMContentLoaded', _mn.domReady, false);